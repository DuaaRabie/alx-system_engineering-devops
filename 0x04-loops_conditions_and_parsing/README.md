This project to be familiar with:
- SSH keys
- The shell loops
- The shell condition statements
- files and comparison operators
- env/ cut/ for/ while/ until/ if
- using #!/usr/bin/env bash over #!/bin/bash
- use Shellcheck tool
